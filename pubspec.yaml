name: pos_woocommerce
description: "POS integrado con WooCommerce para ventas"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Dependencias HTTP y almacenamiento
  http: ^1.1.0
  shared_preferences: ^2.2.2
  
  # Gestión de estado
  provider: ^6.1.1
  
  # Utilidades
  intl: ^0.19.0
  
  # Iconos (requerido)
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Assets opcionales (comenta si no los tienes)
  # assets:
  #   - assets/images/
